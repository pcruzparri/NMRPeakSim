[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nmrpeaksim"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy", "dearpygui"]

[project.scripts]
nmrpeaksim = "nmrpeaksim.gui.main:main"
